<!DOCTYPE html>
<html lang="my">
<head>
    <meta charset="UTF-8">
    <title>ရက်ချုပ်စာရင်း</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .main-content { 
            margin-left: 280px; 
            padding: 20px; 
            min-height: 100vh;
            background-color: #f8f9fa;
        }
    </style>
</head>

<body class="d-flex">
    <%- include('../partials/sidebar') %>

    <div class="main-content w-100">
        <div class="d-flex justify-content-between align-items-center">
            <h4>📅 ရက်ချုပ်စာရင်းကြည့်ရန်</h4>
            <span class="badge bg-info text-dark">ရက်စွဲ - <%= dateFilter %></span>
        </div>
        <hr>

        <form action="/report/daily" method="GET" class="row g-3 mb-4 bg-white p-3 shadow-sm rounded">
            <div class="col-md-3">
                <label class="form-label">ရက်စွဲရွေးပါ</label>
                <input type="date" name="date" class="form-control" value="<%= dateFilter %>">
            </div>
            
            <div class="col-md-3">
                <label class="form-label">အမျိုးအမည် (Item Name)</label>
                <input type="text" name="itemName" class="form-control" placeholder="ဥပမာ - မြရော"
                       value="<%= typeof nameFilter !== 'undefined' ? nameFilter : '' %>">
            </div>

            <div class="col-md-3">
                <label class="form-label">အမျိုးအစား (Type)</label>
                <input type="text" name="type" class="form-control" placeholder="ဥပမာ - ပဲ"
                       value="<%= typeof typeFilter !== 'undefined' ? typeFilter : '' %>">
            </div>

            <div class="col-md-3 d-flex align-items-end">
                <button type="submit" class="btn btn-primary w-100">ရှာဖွေမည်</button>
            </div>
        </form>

        <div class="table-responsive bg-white p-3 shadow-sm rounded">
            <table class="table table-hover align-middle">
                <thead class="table-success">
                    <tr>
                        <th>တောင်သူ</th>
                        <th>အမျိုးအမည်/စား</th>
                        <th>အလေးချိန်</th>
                        <th>ဈေးနှုန်း</th>
                        <th class="text-end">ရှင်းငွေ</th>
                        <th class="text-center">လုပ်ဆောင်ချက်</th> </tr>
                </thead>
                <tbody>
                    <% let pageTotal = 0; %>
                    <% if (records.length > 0) { %>
                        <% records.forEach(row => { %>
                            <% pageTotal += row.total_clearance; %>
                            <tr>
                                <td><%= row.farmer_name %></td>
                                <td><%= row.item_name %> (<%= row.item_type %>)</td>
                                <td><%= row.weight %> ပိဿာ</td>
                                <td><%= row.price.toLocaleString() %></td>
                                <td class="text-end text-success"><strong>
                                    <%= row.total_clearance.toLocaleString() %>
                                </strong></td>
                                
                                <td class="text-center">
                                    <div class="d-flex gap-2 justify-content-center">
                                        <a href="/edit-record/<%= row.id %>" class="btn btn-warning btn-sm">ပြင်မည်</a>
                                        
                                        <form action="/delete-record/<%= row.id %>" method="POST" onsubmit="return confirm('သေချာပါသလား?')">
                                            <button type="submit" class="btn btn-danger btn-sm">ဖျက်မည်</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% }) %>
                        <tr class="table-secondary">
                            <td colspan="4" class="text-end"><strong>စုစုပေါင်း ရှင်းငွေ</strong></td>
                            <td class="text-end text-danger"><strong><%= pageTotal.toLocaleString() %> ကျပ်</strong></td>
                            <td></td> </tr>
                    <% } else { %>
                        <tr>
                            <td colspan="6" class="text-center py-4">စာရင်းမရှိသေးပါ။</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>